/**
 * Placeholder "As seen on Instagram/TikTok" strip.
 * Disabled until real customer content exists — enabling this before real
 * UGC would imply customer endorsements we cannot back up.
 */
export function UgcStrip() {
  return null;
}
